<% if user_signed_in? %>
  <% if post.goods_by?(current_user) %>
    <%= link_to post_goods_path(post), method: :delete do %>
      <i class="fas fa-heart" style="color: red;"></i>
      <%= post.goods.count %>
    <% end %>
  <% else %>
    <%= link_to post_goods_path(post), method: :post do %>
      <i class="fas fa-heart"></i>
      <%= post.goods.count %>
    <% end %>
  <% end %>
<% end %>