<p>アーティスト一覧</p>
<p>各アーティスト名をクリックで詳細ページへ</p>

<h5 class="text-left"><strong>アーティスト一覧</strong><span></span></h5>
<div class="row justify-content-center">
  #<% @artists.each do |artist| %>
    #<div class="col-md-3 col-sm-6 col-12 text-center">
      <!-- アーティストイメージ variant：サイズ変更 processed：未処理画像でもエラーにならないようにする -->
      #<%= link_to artist_path(artist) do %>
        #<%= image_tag artist.image.variant(resize: "200x200").processed %>
      #<% end %><br>

      #<%= artist.name %><br>

    #</div>
  #<% end %>

</div>
<div class="row justify-content-center">
  <div class="d-flex justify-content-center">
    <%= paginate @artists %>
  </div>
</div>